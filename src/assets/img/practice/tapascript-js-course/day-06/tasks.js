// Q1.

function celciusToFarenheit(celcius) {
  return (celcius * 9) / 5 + 32;
}

const calcFarenheit = console.log(celciusToFarenheit(50), "farenheit");

// Q2.
function findMaxNumber(a, b) {
  if (a > b) {
    return a;
  } else {
    return b;
  }
}

console.log(findMaxNumber(-6, -10));

// Q3

function isPalindrome(str) {}
